# 同桌 · 双模式联机桌游馆

一套可直接运行的房间制网页桌游大厅，目前支持《马尼拉》和经典基础版《璀璨宝石》。两种模式共用登录、大厅、房间码、聊天、断线恢复、服务器主动推送、人机和决策倒计时，并分别使用独立的服务端规则引擎。

首页采用统一的“同桌 · TABLE CLUB”入座入口；进入大厅后可在两张实体桌游托盘间选择模式，支持人数、核心玩法与建房选项会同步切换。马尼拉桌面使用结构化航道、货物、船只、特殊区域和秘密股票组件，素材只负责实体桌游质感，所有棋位、数值与操作仍由实时状态生成，不再以整张示意图覆盖界面；璀璨宝石则保留独立的卡牌与筹码桌面。

## 启动

需要 Node.js 20 或更高版本；首次运行先安装锁定依赖。

```bash
npm ci
npm start
```

本机打开：`http://localhost:4173`

### 马尼拉正式三维棋盘

从大厅进入马尼拉对局后，默认使用实时 3D 棋盘。货船缩小至原 Demo 的 70%，常驻标签和下方信息卡明确显示航道点位、13 海盗点或港口／修船厂去向；点击船只或信息卡查看船员、费用和离正常抵港的距离。

所有货物、船位、船员颜色及圆形放置位都由服务器房间数据驱动。发光的可用圆位支持点击派遣；也可展开「全部放置位、费用与操作」使用键盘操作。竞价、布船、骰点、领航、海盗、股票、结算仍使用已有服务端规则和多人同步。刷新恢复当前对局，不会使用演示数据；切换视角不影响其他玩家。

棋盘支持俯瞰、轻拖微调、低动态效果偏好和小屏操作。若 WebGL 不可用或渲染中断，会自动进入兼容棋盘；也可手动点「兼容模式」。3D 资源与脚本由项目本地提供，没有第三方 CDN 依赖。

### 保留的独立视觉 Demo

打开 `http://localhost:4173/manila-3d-demo.html` 可查看独立的三维桌面演示。海图贴在有厚度的棋盘上，船体、弧帆、船员、骰子、硬币、灯具均为三维模型，具有灯光、材质反射与实时投影。

点击「掷骰航行」体验骰子旋转、结果揭晓与货船移动；点击船只可查看示例船员，拖动可微调视角，「俯瞰桌面」和「重新布桌」分别切换镜头与恢复演示。页面使用固定示例局面，仅供视觉评审，不连接房间 API，也不替换正式联机界面。资源和 Three.js 均由本地项目提供。

同一局的其他玩家可在同一局域网打开：

```text
http://房主电脑的局域网IP:4173
```

### 临时公网联机（仅用于测试）

项目已在 `tools/cloudflared` 安装 Cloudflare Tunnel。先保持 `npm start` 运行，再打开另一个终端执行：

```bash
npm run tunnel
```

终端会给出一个临时的 `https://*.trycloudflare.com` 地址。把这个地址和房间码发给朋友即可。游戏期间需要保持两个终端运行，并避免电脑休眠。

临时 Tunnel 需要经过中转节点，适合短时测试，不建议用于正式对局。

### 稳定公网联机

推荐把服务部署在靠近玩家的常驻云主机或容器平台。中国大陆玩家若已有 Sealos 等平台的国内区账号，可优先选择国内节点和平台生成的公网地址；否则选择香港节点。其他地区按玩家所在地选择最近节点。部署时：

- 启动命令：`npm start`
- 服务端口：环境变量 `PORT`（默认 `4173`）
- 健康检查：`GET /healthz`
- 持久目录：把可写磁盘挂载到 `/data`，并设置 `DATA_DIR=/data`

项目提供了 `Dockerfile`，可直接构建容器。服务端通过 SSE 主动通知桌面变化；只有在长连接不可用时，浏览器才降级为每 5 秒同步一次。

在已经安装 Docker 的云主机上，可直接运行：

```bash
docker compose up -d --build
```

部署时固定为 **1 个实例**；当前房间状态保存在挂载卷中，多实例会造成玩家落到不同房间状态。平台生成的 HTTPS 公网地址可以直接分享给朋友。

## 游戏模式

在大厅创建房间时先选择模式，座位数量会自动切换：

- 《马尼拉》：3–5 人
- 《璀璨宝石》：2–4 人

公开房间列表与候场房间都会显示当前模式。

## 《马尼拉》已实现规则

- 3–5 人；每人 30₱、2 张秘密股票；3 人局 4 名帮手，4–5 人局 3 名帮手
- 竞拍港务长，退出竞拍后不可返回；允许用股票贷款支撑竞价。当前最高报价会锁定领先者的融资额度，不能通过赎回股票降低支付能力；异常报价会自动作废并重新竞拍，避免卡局
- 港务长每航程可买 1 股，选择 3 种货物，并设置总和为 9 的三个起点
- 人参、肉豆蔻、丝绸、翡翠四类股票与 0/5/10/20/30 黑市轨道
- 三轮派遣与移动；3 人局第一次移动前连续派遣两轮
- 三艘货船、按费用顺序占用的船员位、货物奖金均分
- 港口 A/B/C、船厂 A/B/C 的费用、条件与 6/8/15₱ 回报
- 海盗船长/海盗：第二轮停 13 时登船，第三轮停 13 时抢劫并决定去向
- 小/大领航员：第三次掷骰前移动 1 艘或 2 艘船
- 保险公司：立即领取 10₱，承担所有失败船的修理费
- 抵押股票获得 12₱，支付 15₱ 赎回；破产时支持偷渡客放置
- 抵港货物涨价；任一货物到 30₱ 后按现金 + 股票市值 − 未偿贷款结算
- 服务器生成骰点、服务端校验行动、版本冲突保护、秘密股票按玩家隔离
- 房主可在开局前手动添加/移除人机；人机会自动判断竞拍、布船、派遣、移船、海盗与领航决策
- 建房时可选择每次决策限时 10/20/30 秒；所有玩家同步看到倒计时，超时后服务器随机执行一个合法动作
- 骰子旋转揭晓、海盗登船/抢船、黑市涨价与终局动效由服务端事件统一触发，所有玩家同步播放
- 行动权切换到本人时显示页面闪烁提醒；支持的移动浏览器同时短振动，后台标签页标题会闪烁
- 每次航程结束后暂停在全屏收支结算页，逐人列出期初现金、每笔收入/支出、本轮净变化与结束现金
- 结算页仅能由本轮大领航员确认；没有大领航员时依次由小领航员、港务长确认，人机确认人会短暂停留后自动继续
- 大厅和游戏内均可退出房间；任意一名真人退出后，当前房间与对局立即解散

## 《璀璨宝石》已实现规则

- 经典基础版完整 90 张发展卡与 10 名贵族，按人数设置普通宝石和贵族数量
- 拿 3 枚不同色宝石；库存不足 3 种时拿取所有现存颜色
- 同色拿 2 枚时校验拿取前库存至少为 4
- 明牌预留与牌堆盲预留、每人最多 3 张、黄金库存不足时仍可预留
- 购买明牌或自己的预留卡；永久折扣优先抵费，黄金自动补足任意颜色
- 实体筹码最多 10 枚，超限时由玩家精确选择归还筹码
- 贵族只检查已购买发展卡；同时满足多名时由玩家选择 1 名
- 15 分触发终局并打完当前轮；最终先比较声望，再比较已购买发展卡数量
- 对手看不到玩家预留卡内容；服务端牌堆不会发送给浏览器
- 专属电脑珠宝商策略与超时合法行动托管

## 操作流程

1. 每位玩家输入自己的昵称。
2. 一人创建房间并把 6 位房间码发给其他人。
3. 其他玩家加入；人数不足时房主可添加人机，达到所选模式最低人数后开始。
4. 《马尼拉》的当前可选位置会在棋盘上发光；《璀璨宝石》可从银行、市场与预留区直接行动。
5. 每次航程结束时，所有玩家会看到本轮收支明细；指定领航员确认后才会进入下一航程或最终排名。
6. 房间名称不可重复；进入房间后，必须先退出并解散当前房间，才能创建或加入其他房间。
7. 刷新或短暂掉线不会丢失对局，用原浏览器重新打开即可回来。

## 测试

```bash
npm test
```

测试同时覆盖两种模式。《马尼拉》覆盖 3 人局节奏、人机、全员同步事件、港口顺序、海盗、收支结算、保险、抵押与股票隐私；《璀璨宝石》覆盖完整牌库、人数配额、拿取限制、超限归还、预留隐私、黄金支付、贵族选择、同轮终局、人机与超时托管。

## 项目结构

```text
lib/game.js          《马尼拉》服务端规则状态机
lib/splendor.js      《璀璨宝石》服务端规则状态机
lib/splendor-data.json  90 张发展卡与 10 名贵族数据
server.js            HTTP、房间、会话、SSE 推送 API、静态文件服务
public/               游戏前端
test/                 两种模式与服务端集成测试
Dockerfile            常驻容器镜像
compose.yaml          单实例与持久卷配置
```

### 墨斗 · 在线格斗 1.0

打开 `/duel.html` 或从同桌大厅进入墨斗。界面分为格斗大厅、等待房间和独立战场；玩法说明在大厅弹窗内查看，对战结束可再战或返回房间。支持音效开关、全屏、退出确认和刷新重连。

- 创建房间选火柴人「疾风」或叉叉怪「磐石」，加入者使用另一阵营。分享六位房间码或邀请链接，双方准备后开战。
- 真人对战 99 秒；训练假人不主动行动，训练不限时，可点击「重置训练 · 满气」恢复双方生命、气和位置。
- A/D 移动、W 跳跃、空格冲刺、S 防御、J 四段普攻、U/I 技能、O 满气奥义。桌面键盘为主要操作方式。
- 普攻每次按 J 衔接一段，可在出招中预输入下一段；只缓存一段，长按不会自动打完整套。停顿超过收招后约 0.55 秒、受击或释放其他技能会重置段数。火柴人：推掌 → 两身位疾进 → 后跳投掷手里剑 → 跃起锁定手里剑落点下砸；叉叉怪：磐石推 → 团身滚碾 → 弹跳头槌 → 十字震地。手里剑有独立飞行与碰撞判定，终结段击倒；各段均可格挡。
- 原创角色、遗迹场景、关节动画和独立技能特效；包含浮空、倒地、起身保护和 KO 表现。

- 平衡：100 点生命；四段普攻全中火柴人 12 点、叉叉怪 14 点，较上一版降低约 66%。普攻保留连段压制与回气能力。受击僵直不能移动、冲刺或出招，僵直期间的出招输入会丢弃。
- 技能霸体：U / I / O 从施放到收招抵抗普攻（含手里剑）的僵直、击退与击倒，仍正常扣血，生命归零照常 KO；技能命中仍能打断技能。
- 火柴人：青色「青岚连踢」两段共 18 伤害；紫色「紫电升空斩」22 伤害并腾空挑起；「天隙 · 千影雷葬」锁定地面区域，三次 8 伤害后接 34 伤害雷柱，共 58。
- 叉叉怪：橙色「熔岩破城钻」20 伤害；绿色「地脉晶棘阵」三道不同位置的晶棘各 10 伤害，可跳跃躲避，实际命中数取决于位置；「赤曜 · 陨星灭界」预警后跃起重坠，范围内造成 62 伤害。大招消耗 100 气、无视格挡，具有可躲避的地面落点预警。

战斗优先使用 `/api/duel/socket` WebSocket 长连接。服务端采用单调时钟驱动的 60 Hz 固定步长、30 Hz 状态广播；按键变化立即发送，取消串行 HTTP 往返等待。客户端复用移动和出招逻辑进行最多 150 ms 的本机预测，按服务器输入序号校正，对手位置用约 50 ms 的快照缓冲插值。伤害、气消耗、碰撞与胜负最终由服务器判定。这里的预测是响应优化，并非完整回滚格斗网络协议；距离过远、抖动和丢包仍会影响命中反馈。界面显示实际往返延迟，高延迟时提示网络状况。

WebSocket 无法连通时会自动降级到 SSE 与 HTTP 输入，并继续尝试恢复 WebSocket；降级时界面标注「兼容连接」。服务器前的反向代理须支持 WebSocket Upgrade；SSE 降级需关闭代理缓冲。请使用同一台常驻服务器或对该服务器设置会话粘性，当前格斗房间存在内存中，重启或 30 分钟无操作会失效。房间使用独立随机凭证，不向对方公开凭证；当前没有跨实例房间同步、账号战绩或匹配系统。

两台电脑需要访问相同部署地址。局域网使用主机局域网 IP，公网使用部署域名；不要将 localhost 邀请链接发给另一台电脑。公网反向代理必须转发 `/api/duel/socket` 到 Node 服务，GitHub Pages 静态托管本身不能运行联机服务器。Docker 镜像通过 `npm ci --omit=dev` 安装锁定的 WebSocket 依赖。
